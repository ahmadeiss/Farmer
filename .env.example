# ============================================================
# Smart Hasaad - حصاد الذكي  |  Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================

# ----- Django -----
SECRET_KEY=change-me-to-a-long-random-secret-key-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,backend
DJANGO_SETTINGS_MODULE=config.settings.development

# ----- Database (PostgreSQL) -----
# Database
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'smart_hasaad',
        'USER': 'hasaad_user',
        'PASSWORD': 'hassad_smart@554477',
        'HOST': 'localhost',
        'PORT': '5432',
    }
}

# ----- Redis / Celery -----
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# ----- Django Channels -----
CHANNEL_LAYERS_HOST=redis

# ----- JWT -----
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# ----- CORS -----
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ----- Frontend -----
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=حصاد الذكي

# ----- Media / Storage -----
MEDIA_ROOT=/app/media
MEDIA_URL=/media/

# ----- OTP (Mocked in dev - scaffold for real provider) -----
OTP_PROVIDER=mock
# OTP_PROVIDER=unifonic
# UNIFONIC_APP_SID=your-app-sid
# UNIFONIC_SENDER_ID=SmartHasaad

# ----- Audio Transcription (Whisper scaffold) -----
TRANSCRIPTION_SERVICE=stub
# TRANSCRIPTION_SERVICE=whisper_local
# WHISPER_MODEL_SIZE=base

# ----- Email (Optional) -----
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-sendgrid-key

# ----- QR Code -----
QR_CODE_BASE_URL=http://localhost:8000/api/v1/orders/confirm-qr

# ----- Analytics -----
ENABLE_ANALYTICS_CACHE=True
ANALYTICS_CACHE_TIMEOUT_SECONDS=300
